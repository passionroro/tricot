# 24/10

- Ameliorer la detection de la couleur de separation
    - lorsqu'on detecte les 8 couleurs, a la fin, on peut utiliser le roi jaune pour faire un kmeans et detecter la couleur de separation
    - faire une fonction de debut du kmeans !
~~- Creer un mode verbose en compilant avec l'option `-v`~~

```sh
[*] Run en mode verbose
[ ] Tester la detection des couleurs dans le header
[ ] Modifier la calibration du header
```

- Verifier qu'on utilise bien la bonne lumiere
